# Task 7 C1 final-HEAD Release smoke — 2026-09-13

Controller independently verified local HEAD `ce2efae5566ff769652168d595cce0b161295bf4`: GPUI suite with only the documented unrelated donor skip **1,308 passed / 0 failed / 1 filtered**; both crate `cargo fmt --check`, `git diff --check`, and `cargo build --release --manifest-path packages/app-lite-gpui/Cargo.toml` exited 0. Final Release binary `/Users/kevinhao/Projects/joplin/.shared-target/release/velotype` SHA-256: `13451d9a6725af4e2dda83c43af114ccbdfb3a1602878137669ad2a7ef2aa54c`.

Launched that exact binary with **only** `JOPLIN_LITE_PROFILE=/tmp/joplin-lite-c1-final.NxZQsF`. In the real macOS window, the controller observed an empty library, used Cmd-N to create one note, opened the Cmd-F panel, used Cmd-K to show the independent dimmed global search palette, then Cmd-F to dismiss it and return to the visible in-note panel. Escape closed the panel; Cmd-Q exited process code 0. Screenshots in that disposable directory: `initial.png`, `new-note.png`, `find-query.png`, `cmd-k.png`, `cmd-k-to-f.png`, `find-esc.png`. SQLite `PRAGMA integrity_check` returned `ok`; one note row exists.

Important evidence limit: this Mac had Squirrel/Rime Chinese input selected. AppleScript's ASCII keystrokes were interpreted as provisional pinyin and **did not commit body text**; a populated Release find result, physical Chinese IME commit/candidate navigation, and body-save operation are therefore **not** claimed as passed by this smoke. The mounted tests cover deterministic committed CJK query/results and marked-state branches. The in-app CUA transport returned `Transport closed`, so the window smoke used macOS System Events and a cropped screen capture. No personal Joplin/Evernote profile, NAS, or server was opened or modified.
