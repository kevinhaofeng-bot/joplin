# SDD ledger — plan: docs/superpowers/plans/2026-09-11-evernote-core-notes-roadmap.md

Started: 2026-09-11
Branch: codex/joplin-lite-native-rust
Start commit: 03bd0b800d8c0f0433867485c8af498649635ffc
Spec: docs/superpowers/specs/2026-09-11-evernote-core-notes-product-design.md

## Preflight overlap and consistency scan

| Tasks | Producer / consumer or shared surface | Finding / ruling |
| --- | --- | --- |
| 1 / 2 | Task 1 creates `app-lite-core`; Task 2 adds domain/schema/repository | Clean: Task 2 builds on Task 1 public crate and codec/resource primitives. |
| 1 / 3 | Task 1 adds GPUI path dependency; Task 3 consumes the core from AppModel | Clean: Task 1 must keep the existing spike buildable before Task 3 changes the default route. |
| 1 / 4 | Task 1 creates UI-independent canonical document; Task 4 creates GPUI codec | Clean: core never imports GPUI types; conversion stays in `app-lite-gpui/native_editor/codec.rs`. |
| 1 / 5 | Task 1 produces `ResourceStore`; Task 5 consumes it for image/attachment insertion | Clean: resource bytes remain out of editor elements and HTML. |
| 2 / 3 | Task 2 produces repository/events/list projection; Task 3 consumes them | Clean: selection uses NoteId and list projection excludes body/bytes. |
| 2 / 4 | Task 2 produces journal/snapshot operations; Task 4 consumes them in SaveCoordinator | Clean: Task 4 may add timing/orchestration but not duplicate SQL. |
| 2 / 5 | Task 2 defines resource relations/events; Task 5 uses one cross-layer insert flow | Clean: Task 5 must commit association before publishing card state. |
| 2 / 6 | Task 2 owns organization entities/query; Task 6 owns navigation/UI | Clean: UI emits typed operations; repository remains the data authority. |
| 2 / 7 | Task 2 creates search queue/table baseline; Task 7 owns parser/index workers | Clean: Task 7 may migrate search schema forward but must not bypass repository transactions. |
| 2 / 8 | Task 2 owns schema/resources; Task 8 imports through staging repository | Clean: imports never mutate the live connection while scanning/staging. |
| 2 / 9 | Task 2 creates outbox/cursor/conflict storage; Task 9 owns transport/scheduler | Clean: local entity mutation and outbox enqueue remain one transaction. |
| 3 / 4 | Task 3 creates AppModel and optional session; Task 4 defines real NoteSession | Clean: Task 3 uses a narrow session placeholder interface that Task 4 replaces without changing navigation. |
| 3 / 5 | Task 3 creates note card/list; Task 5 adds thumbnail/attachment updates | Clean: card refresh uses `LibraryEvent`, never direct editor inspection. |
| 3 / 6 | Both touch navigation/list/sidebar/app shell | Clean: Task 3 delivers the minimal two-pane route; Task 6 extends it to organization and pane modes. |
| 3 / 7 | Task 3 owns AppModel route; Task 7 adds search route/UI | Clean: search is a navigation route, not a second list implementation. |
| 3 / 10 | Task 3 establishes product route; Task 10 removes residual spike identity | Clean: measurement-only spike remains reachable until final gate. |
| 4 / 5 | Both touch NoteSession, EditorCore, transactions | Clean: Task 4 establishes durable saves; Task 5 adds the resource transaction through that interface. |
| 4 / 7 | Task 4 owns EditorCore codec/session; Task 7 adds in-note find decorations | Clean: find state is ephemeral and never enters canonical HTML or undo history. |
| 4 / 9 | Task 4 defines explicit flush; Task 9 flushes before sync | Clean: sync consumes `flush(FlushReason::Sync)` and never serializes editor state itself. |
| 4 / 10 | SaveCoordinator state feeds final status UI | Clean: final status presents local save separately from index/sync. |
| 5 / 6 | Task 5 adds thumbnail key/card updates; Task 6 adds list modes | Clean: every mode consumes the same projection and bounded thumbnail service. |
| 5 / 8 | Imported attachments must use Task 1/5 resource contracts | Clean: staging importer writes verified blobs/relations without creating GPUI entities. |
| 5 / 9 | Task 5 resource hashes feed Task 9 transfer | Clean: sync references SHA-256 and resource IDs; note bodies never carry bytes. |
| 6 / 7 | Navigation/filter state is shared | Clean: global search compiles to `LibraryRoute::Search`, preserving NoteId selection semantics. |
| 6 / 10 | Task 6 creates pane motion; Task 10 measures and polishes it | Clean: Task 10 may tune only within the 180–220 ms spec range and cannot recreate views to pass. |
| 7 / 8 | Imported notes/resources must enter/rebuild search index | Clean: Task 8 completes staging verification only after Task 7 index worker reaches a settled state. |
| 7 / 9 | Pulled remote changes must update local index | Clean: remote apply uses repository save path and therefore queues indexing. |
| 7 / 10 | Search/index statuses and latency become release evidence | Clean: Task 10 reports failures instead of folding them into “saved”. |
| 8 / 9 | Imported data needs a sync baseline | Ruling: imported entities start with local revision 1 and explicit unsynced outbox operations only when the user enables the new server; importing alone never uploads data. Cost if wrong: enabling sync later requires a one-time baseline scan. |
| 8 / 10 | Export restore becomes final recovery evidence | Clean: isolated restore checks both database relations and resource hashes. |
| 9 / 10 | Sync status, backup, restore drill and server files overlap | Clean: Task 9 delivers functioning server; Task 10 adds operational recovery and final status evidence. |
| Task 1 self | Tests are specified before a crate exists | Ruling: creating a minimal `Cargo.toml`, `lib.rs` module declarations and empty API shells is permitted setup for the RED run; no codec/resource behavior is implemented before the expected failing assertions. Cost if wrong: one setup commit may mix scaffolding with the first TDD cycle. |
| Task 1 self | “Move, do not rewrite” versus TDD | Ruling: this is a behavior-preserving extraction of already tested production code; first copy the existing characterization tests and observe the new crate fail, then move the implementation and retain old-crate regression runs. Cost if wrong: extraction can preserve legacy quirks that a new implementation might have removed. |
| Task 2 self | Schema v4 includes tables whose behavior lands in later tasks | Clean: Task 2 creates durable storage contracts only; later tasks implement workers and transport. |
| Task 3 self | Default route changes while spike is a stable measurement baseline | Clean: keep `--evernote-spike` unchanged and add a separate default AppModel path. |
| Task 4 self | Journal and snapshot timing require deterministic tests | Ruling: inject a clock/scheduler seam into SaveCoordinator; do not use sleeps in unit tests. Cost if wrong: a small abstraction remains in production solely to make timing deterministic. |
| Task 5 self | M1 asks both images and generic attachments | Clean: image correctness is load-bearing; generic cards can be minimal but must persist/open correctly. |
| Task 6 self | Full organization and animation share a task | Ruling: implement organization/navigation first, then motion as a final isolated commit within Task 6; review package covers both. Cost if wrong: Task 6 may need two review passes. |
| Task 7 self | OCR/PDF platform work is larger than basic FTS | Ruling: ship title/body/filter search first and keep OCR/PDF as the second isolated commit in Task 7; M2 search acceptance distinguishes immediate index from extractor completion. Cost if wrong: OCR adapter issues can delay Task 7 completion without blocking local note safety. |
| Task 8 self | Historical counts `31/64/4,238` may drift | Ruling: use a fresh read-only source scan as acceptance authority; those numbers are the scale fixture and prior baseline, not hard-coded truth. Cost if wrong: the migration report can differ from an older document while remaining correct for the current source. |
| Task 9 self | Loro is conditional, not the initial sync dependency | Clean: first deliver revision preconditions and visible conflicts; add Loro only with measured benefit and readable recovery. |
| Task 10 self | Product completion requires real Release evidence | Clean: unit/build success alone cannot close the task or create a release tag. |

## Task progress

Baseline: linked worktree verified at `/Users/kevinhao/Projects/joplin/.worktrees/joplin-lite-native-rust-mvp`, branch `codex/joplin-lite-native-rust`, clean at `03bd0b800`.

Baseline: `packages/app-lite-native` 225/225 tests pass (133 lib + 85 bin + 7 lifecycle).

Baseline: unfiltered `packages/app-lite-gpui` reproduces the documented donor SIGSEGV in `editor::selection::tests::cross_block_cut_writes_markdown_deletes_range_and_undo_restores`; focused rerun also exits signal 11. `UPSTREAM.md` and the prior editor plan already record this exact unchanged donor failure.

Baseline: GPUI suite with the exact documented skip passes 996/996, 0 failures, 1 filtered.

Task 1: implementer `/root/m1_task1_core_extract_terra`, base `03bd0b800d8c0f0433867485c8af498649635ffc`; implementation and self-test report pending.

Task 1 review round 1: `CHANGES REQUESTED` at `09892578b006ca5e35e17ca81eb8092200d25800`; 0 Critical, 4 Important. Rulings: close `CanonicalDocument` construction invariants before Task 4 consumes it; split 32-hex logical `ResourceId` from 64-hex `BlobHash`; remove the public unverified blob path; replace integrity and interrupted-publication false-positive tests with mutation-sensitive tests and a narrow deterministic failpoint.

Task 1 fix round 1: commit `7f29a7e5420f986e65a5da3d3e7ccf32e7b068b8`; core 56/56, native 225/225, GPUI check PASS and 996/996 with only the exact known donor SIGSEGV skipped.

Task 1 re-review: `APPROVED`; 0 Critical, 0 Important, 0 Minor. Task 1 complete at `7f29a7e5420f986e65a5da3d3e7ccf32e7b068b8`.

Task 2: implementer `/root/m1_task2_library_repository_terra`, base `7f29a7e5420f986e65a5da3d3e7ccf32e7b068b8`; implementation and self-test report pending.

Task 2 implementation: commit `079044e504cfa323844817fb473da4d4608dd31c`; core 64/64, native 225/225, GPUI check PASS and 996/996 with the exact known donor skip.

Task 2 review round 1: `CHANGES REQUESTED`; 2 Critical, 11 Important, 3 Minor. Critical rulings: fail closed and leave schema v3 untouched when legacy `markup_language=1` RTF is present (the dedicated backed-up conversion belongs to Task 8); change resource relations to occurrence semantics so `A,B,A` round-trips. Important rulings: all eleven findings are in-scope because they are explicit product/repository contracts; close them before Task 3. Minor error-message polish may remain if it does not erase the underlying source/category, but PRAGMA verification and `BlobHash` typing are required now.

Task 2 fix round 1: commit `486024a3a64cc1c17eb2f67ffdc81105f11713ec`; core 76/76, native 225/225, GPUI check PASS and 996/996 with the exact known donor skip.

Task 2 re-review round 1: `CHANGES REQUESTED`; 1 Critical, 5 Important, 2 Minor. Rulings: legacy RTF gate must occur under the migration write lock before WAL/profile side effects; all durable IDs use the repository allocator and reserve retired identities; purge must retain a durable search-delete job; rollback of an associated resource is a complete no-op; bind database/resources to one stable profile directory identity; replace file-hash migration assertions with write-authorizer/complete schema-data-profile assertions. Hide clock/ID seams behind non-default test support or crate-private tests and retain entropy error sources.

Task 2 fix round 2: commits `f830764ac3a8c5888e9116538d178c2ca32889a5` and `bdf8d710aa25137fd6bc48af9bd4aec6a4708313`; added migration locking, zero-write RTF refusal, durable purge search-delete job, stable profile FD binding, outbox ID retry, two-connection race tests, real profile swaps, v4 authorizer zero-write gate and complete v3 failure snapshots. Implementer passed default/test-support core, native 225 and GPUI check; controller freshly ran the exact GPUI skip suite at `bdf8d710a`: 996 passed, 0 failed, 1 filtered.

Task 2 re-review round 2: `CHANGES REQUESTED`; 2 Critical, 5 Important, 1 Minor. Rulings: set/verify WAL before schema publication after an early read-only legacy probe, repeat the authoritative legacy check under `BEGIN IMMEDIATE`, and restore the prior journal mode on refusal/failure; repair/retry WAL on every v4 open. Move `BeforeMigrationCommit` before the final profile identity verification and verify again before return. Bind the lexical profile directly with `O_NOFOLLOW` before canonicalization. Retry all entity inserts at the actual constraint boundary. GC only uniquely owned unassociated blob metadata. Track preflight-created `resources` and `blobs` directories independently. Expand failure snapshots and purge reopen/consume/rollback tests.

Task 2 fix round 3: commit `dc506fa4fa8d0a58a66d6c461a5295794e6ec554`; default core 69, test-support core 89, native 225, GPUI check and exact-skip suite 996/996 PASS.

Task 2 re-review round 3: `CHANGES REQUESTED`; 2 Critical, 0 Important, 3 Minor/deferred. Three normal fix rounds are exhausted, so the controller escalated to a fresh implementer and an architectural publication-boundary repair. Required closure: bind/compare the database child identity across SQLite's pathname open, and make schema commit the no-fail publication boundary with no ordinary `Err` after commit. Deferred non-blocking hardening: cross-process `SQLITE_BUSY_SNAPSHOT`, constructor-edge directory cleanup, and additional mutation-test breadth.

Task 2 publication rescue: commit `8741bb7f374e56605d2fef2cddea88759c165013`; default core 69, test-support core 91, native 225, GPUI check and exact-skip suite 996/996 PASS. Re-review closed the no-fail commit boundary but found one remaining Critical: pathname re-stat cannot prove the SQLite VFS actually opened the held inode under a regular-file ABA, and new-file cleanup uses a non-atomic `fstatat` then `unlinkat`. Final ruling: use SQLite's `SQLITE_FCNTL_HAS_MOVED` against the live main connection and add a deterministic A->B->A test; remove destructive failed-claim unlink rather than risk deleting a replacement.

Task 2 final publication fix: commit `c774c7ba70b3e7b244927bd23e16e1f414c0d4a3`; default core 69, test-support core 93, native 225, GPUI check and exact-skip suite 996/996 PASS. The live main connection is now checked with SQLite's public `SQLITE_FCNTL_HAS_MOVED` contract before schema publication, the deterministic A->B->A ABA regression proves zero side effects, and destructive failed-claim unlink cleanup was removed.

Task 2 final re-review: `APPROVED`; 0 Critical, 0 Important, 3 unchanged deferred Minor. Task 2 complete at `c774c7ba70b3e7b244927bd23e16e1f414c0d4a3`. Deferred hardening remains cross-process `SQLITE_BUSY_SNAPSHOT` whole-transaction retry, constructor-edge directory cleanup, and broader mutation-test coverage; none blocks the single-repository Task 3 application shell.

Task 3 implementation and repair: commits `f1b4b2d85` through `17318ae22`; default route now opens the native GPUI notes-library shell, shares the native editor surface, uses projection-only virtualized lists, restores persisted pane/selection state atomically, and routes UI/menu/events through one reducer-observer chain. Three repair rounds closed editor mounting, list tail access, lifecycle, notification, snapshot, hydration, event-cancellation, paint/read-only/resource evidence, and partial-commit error integrity.

Task 3 final re-review: `APPROVED`; 0 Critical, 0 Important, 0 Minor. Independent verification: core 73, native 225, GPUI 1,048 passed with only the exact pre-existing donor SIGSEGV test filtered; all-target check, release build, fmt/diff, default-profile SQLite integrity, and empty/typical spike smoke passed. Task 3 complete at `17318ae229e1906ad9171ac440f781f472624fda`.

Task 4 implementation and repair: commits `82f64741b` through `a91b842da`; the library now mounts an editable retained `NoteSession` with native title/body input, deterministic two-way canonical codec, readable delta journal, settled/hard-cap snapshots, exact lifecycle flush barriers, background persistence, IME candidate isolation, database writer ownership and crash recovery across v4→v5 migration.

Task 4 final re-review: `APPROVED`; 0 Critical, 0 Important, 1 deferred Minor. Independent verification: core 81, native 225, GPUI 1,081 passed with only the exact pre-existing donor SIGSEGV test filtered; migration 13, NoteSession 22, UI 28, all-target check, release build, fmt/diff, default-profile integrity and empty/typical spike smoke passed. Deferred hardening: legacy journal size validation occurs after SQLite/Serde allocation. Task 4 complete at `a91b842da`.

Task 5: implementer `/root/m1_task3_integration_rescue_terra`, base `a91b842da97ee48db2f05e7bf0cacfd6e637a020`; implementation remains in the shared dirty worktree and is not yet committed.

Task 5 evidence gate: the plan now requires every task to re-read the mapped Evernote source before implementation and review, and every report must provide Evernote source path/symbol/observed behavior -> Rust file/function -> mutation-sensitive verification. Unsupported behavior is labeled as an independent product decision.

Task 5 pre-review snapshot: 1 Critical and 5 Important. Closed so far: canonical transaction-selected thumbnail result; atomic gap/tail paragraph activation; full tracked Selection mapping across prefix edits, selected-range replacement, split/merge/removal; NoteId mismatch rejects without mutating the destination note. Remaining before review: retained background resource prepare/commit, real attachment select/open/error interaction, and mounted EditorSurface event-path matrix.

Task 5 current implementation checkpoint: the shared dirty worktree now has the complete local resource flow: background staged resource ingest plus one atomic note/resource/projection commit; full Selection/NoteId-mapped picker, paste and drop insertion; inline image residency/hydration with placeholder-first geometry; attachment cards with atomic selection and verified system handoff; resource deletion/undo/redo journal recovery; and a canonical selected-thumbnail outcome applied in the current presentation frame. Legacy image dimensions retain an explicit native `natural_size_known` state, so an offscreen old image cannot persist the 1024×768 layout fallback before successful visible hydration repairs it. Native image representations/path/data URI classification are bounded and defer verification/decode to retained workers. Attachment handoff uses a worker-owned 0700/0600 lease and absolute `/usr/bin/open.status()`; generic system-open policy, journal ownership/provenance, resource budgets and active+latest hydration queue are documented as independent local-product architecture rather than claimed Evernote line-for-line behavior. Evidence crosswalk: `.superpowers/sdd/2026-09-11-evernote-core-notes-roadmap/task-5-report.md`.

Task 5 current verification: core default 85/85 and `--features test-support` 111/111 PASS; GPUI `cargo check --tests`, fmt/diff PASS; independent GPUI bin full run with only the exact known donor `cross_block_cut_writes_markdown_deletes_range_and_undo_restores` skip: 1,159 passed / 0 failed / 1 filtered. Release 截图复核发现此前实机启动的是陈旧 `packages/app-lite-gpui/target/release/velotype`，并非 Cargo metadata 指向的 `.shared-target/release/velotype`；所以早先“fresh profile 已通过”的结论撤回。当前 source-backed 修复将 `--color-background-fill-primary` / `--color-surface-fill-primary-enabled`（`--colors-grey-100 = #fff`）、primary/secondary text 和 primary stroke 应用于 shell/main/actions/title/pane、empty/no-selection/unsupported、shared Library Chrome 与 body surface；分发脚本也通过 Cargo metadata 取新 binary。新的 `mounted_default_light_route_keeps_every_editor_state_opaque_and_contrasted` 经空态、无选择和选中态的实际 render seam 验证，并在生产 primary surface 临时改为黑色时 RED。**Task 5 Step 5 仍待新的 shared-target Release 手工验收，未勾选。**

Task 5 light-route Release correction (2026-09-12): after the explicit surface/text/stroke hardening and metadata-target packaging fix, current GPUI `ui::tests` is 53/53, `app::tests` 72/72 and full exact-donor-skip is 1,170/0/1; check/tests, two-crate fmt, diff and shared-target Release build all PASS. This proves the current source/build artifact, not the prior stale local binary; **the fresh-profile visual M1 gate remains pending and Step 5 is still unchecked**.

Task 5 format Chrome checkpoint (2026-09-12, shared dirty worktree, uncommitted): `native_editor::toolbar::EditorCommandChrome` is now the sole formatting owner for both `--evernote-spike` and the default `LibraryShell`, using the active `NoteSession`'s existing `EditorCore`. The library mounts it between `library-note-title` and the native body, destroys it with session/surface on selection/switch/codec failure, and routes typed `RequestInsertImage` through saved `InsertIntent` then the existing staged picker completion—never the Spike direct route. Actual donor crosswalk and test evidence are in `task-5-format-chrome-brief.md` / `task-5-format-chrome-report.md`: `peso/commands.ts` supports a shared command catalogue; `textformatter/commands/boolformat.ts::execCommand/queryCommandValue` supports selection-derived single history transactions and focus restoration; the targeted reverse notes support one toolbar/More placement system with preserved selection. Mounted PASS: default shared mount/structural placement, true Bold+list selection/one-undo, ManualSync→switch/reopen canonical Bold, narrow More execute, Link focus/selection + switch teardown, and typed InsertImage→staged durable relation. **初版仅让 More/Link 自身 `.occlude()`，独立复审仍发现 outside body click 会先进入 Library `EditorSurface` capture 并遗留 overlay；该说法已撤回。** 现已在共享 Chrome 任一 overlay 打开时先挂透明全窗口 `.occlude()` backdrop，且 More/Link panel 排在其上；outside click 与 Escape 都只经共享 `dismiss_overlay`，关闭后 selection/history 不变并回 editor focus。进一步复审的 stale-menu 缺口也已闭合：共享 `EditorCore` observer 比较完整 `Selection`，只在实质 selection change 时关闭 More；Link 的独立 URL input 保留 pinned document selection，输入不会误关。Library 与 Spike 各有真实 `right` 键 route 的突变敏感测试，均断言无 history corruption；Spike 另有 More→Escape 测试，临时删除 `SpikeView` root `.on_key_down` 后按预期 RED，证明不是 LinkPopover 的 Escape 假覆盖。`ui::tests::` 49/49、`spike_app::tests::` 46/46、完整 GPUI exact-donor-skip 1,159/0/1 PASS。独立复审为 `APPROVED`（0 Critical / 0 Important / 1 deferred Minor）；但 fresh-profile Release 手工流程因陈旧 local binary 回归必须重新执行，不能视为已通过。

Task 6 source-first checkpoint (2026-09-12): before organization/navigation implementation, the controller re-read the reconstructed Evernote main modules for typed nav state (`51113`), note-list selection/width (`74300`), list modes (`76905`), All Notes/Trash defaults (`74643`/`35172`), SQL query composition and tag intersection (`38218`), hierarchical tags (`35422`), notebooks/stacks/recents (`63570`), shortcuts (`98783`/`95680`), shared note menu actions (`00951`) and navigation history (`54193`/`62264`). The resulting Rust contracts and mutation-sensitive acceptance matrix are frozen in `task-6-source-brief.md`; Task 6 implementation has not started and must follow that brief.

Task 6 Stage A (2026-09-12, shared dirty worktree, uncommitted): core/state/query foundation is now implemented without mounting any new sidebar/list UI or pane motion. `LibraryRoute` is the sole query truth (`AllNotes`, `Notebook`, `Stack`, canonical multi-tag AND, `Trash`); `SortSpec` defaults Updated-desc for normal routes and Deleted-desc for Trash; `ListQuery::paged` compiles bounded SQL projection pages (maximum 500) with SQL Stack subquery, multi-tag `HAVING` intersection, Trash isolation, NOCASE title ordering and stable ID tie-break. Existing Task 3 virtual-list bootstrap intentionally remains compatibility-only `for_route` until a later incremental paging UI phase, so its 1,662-note tail is not truncated. `NavigationState` now owns route-scoped sorts plus native typed `NavigationHistory` snapshots (`LibraryRoute + Option<NoteId>`); Back/Forward restores IDs, new navigation truncates Forward, and projection refresh does not create history. Fix round 1 makes NavigateTo/Back/Forward/SetSort candidate/prepare/commit operations: repository query, target hydration and changed selection persistence must all succeed before one nonfallible live navigation/projection/session swap. Query fixtures now explicitly cover Notebook predicate, exact non-overlapping SQL pages, 0/501/signed-offset bounds, reverse-insertion stable tie-break, and all five route defaults; history executes real Forward before branch truncation. RED→GREEN mutation tests: core 5 Stage-A tests; app 5 failure-atomicity tests plus Forward restoration. Current verification: core default 85/85, core `test-support` 116/116, GPUI app tests 72/72, GPUI `cargo check --tests`, `cargo fmt --check`, and `git diff --check` all PASS. Evidence crosswalk: `task-6-stage-a-report.md`.

Task 6 Stage B1 (2026-09-12, shared dirty worktree, uncommitted): the approved typed route/query/navigation core is now mounted as an actual three-column LibraryShell MVP. `LibraryNavigationIndex` supplies only typed `Notebook`/`Stack`/`Tag` metadata from one read-only SQLite snapshot; `ui/sidebar.rs` renders All Notes, stack+nested notebook, tag, and Trash rows and sends all clicks through `AppAction::NavigateTo`. The card pane remains the single `AppModel::projections`/`uniform_list` authority; Cards/Snippets/Compact change only presentation and retain the selected NoteId. Collapsing sidebar then list reaches three/two/one-column states while retaining the same `NoteSession` and `EditorSurface` entity IDs, so no body/image rehydration occurs. New mutation-sensitive core/UI tests cover typed metadata/tree ordering, all five sidebar route classes with exact projection IDs and zero body/resource blob reads even with a thumbnail fixture, mode order/selection/session continuity, and the 1,662-tail initial mount body-load gate. Verification: core default 85/85, core test-support 117/117, GPUI app 72/72, UI 51/51, complete GPUI exact-donor-skip 1,168/0/1, check/fmt/diff PASS. No motion, CRUD/bulk controls, shortcuts, recent items, search, multi-tag chooser, or Top List was added. Evidence crosswalk: `task-6-stage-b-report.md`; B1 remains uncommitted and awaits independent review.

Task 6 Stage B1 review-fix checkpoint (2026-09-12, shared dirty worktree, uncommitted): review I1 replaced the clipped sidebar column with a retained GPUI `UniformListScrollHandle` plus bounded `uniform_list` ranges. A mounted 760px scale fixture uses the real default+30 notebook / 64 tag tree, proves tail Tag and Trash are initially outside the viewport, requests each tail range through the production scroll handle, then clicks it and asserts exact typed route/selected state with no body or resource read. Review I2 adds a `test`/`test-support`-only SQLite `AuthAction::Read` observer to `list_navigation_index`; its allow-list rejects `notes.body_html`, `notes.body_text`, `notes.merge_state`, `resource_blobs.bytes`, and every other non-sidebar column even if a future query discards its result. The fixture soft-deletes one stack, notebook, and tag and asserts none reappear. A temporary discarded `SELECT body_html FROM notes LIMIT 1` made the exact authorizer target RED with `notes.body_html` in the observed set, then the source mutation was removed and the target returned GREEN. Current verification: core default 85/85; core test-support 118/118; Stage-A core query target 7/7; GPUI app 72/72; UI 52/52; complete exact-donor-skip 1,169/0/1; check/fmt/diff PASS. B1 remains uncommitted and requires independent re-review; no Task 1–5 editor/save/resource path, motion, CRUD/bulk controls, shortcut/recent/search, multi-tag chooser, or Top List was changed.

Task 6 Stage B1 review-fix round 2 (2026-09-12, shared dirty worktree, uncommitted): the sidebar scale probe is now test-owned by each `LibraryShell`, not process-global. `ui/sidebar.rs` sends every real uniform-list processor range to the host's `SidebarRenderProbe` only under `cfg(test)`; release builds retain no global mutable instrumentation. The mounted 760px tail fixture now checks each shell-local request/range instead of a cross-test cumulative count: target Tag and Trash ranges contain their durable indexes and remain `<80`. Deleting the processor record call deterministically makes the target RED at `request_count > 0`; restoring it returns GREEN. Parallel `ui::tests` with `--test-threads=8` passed 52/52 for three consecutive runs, then core default 85/85, core test-support 118/118, Stage-A query 7/7, app 72/72, complete exact donor-skip 1,169/0/1, check/fmt/diff all passed. B1 is still uncommitted and pending independent re-review.

Task 6 Stage C1 initial checkpoint (2026-09-12, shared dirty worktree, uncommitted; superseded by the review-remediation entry below): source-first MVP organization mutations now use repository-owned typed Stack/Notebook/Tag commands, typed Move/Tag/Restore/Purge app actions, and one `AppModel::apply_organization_mutation` candidate/prepare/commit boundary. SQLite commits first; only a fully built navigation index, route fallback, card projection, `NoteId` selection and active-session candidate may swap the live model. A candidate failure truthfully reports the committed fact while route/history/projection/session remain unchanged. The mounted LibraryShell has a minimal scrollable organization panel with actual input/buttons for create, context rename/delete, move current note, set/add/remove tags, and Trash-only restore/purge; all controls send the same typed reducer actions. Existing dirty-session handling flushes before a current-note organization revision and the retained observer remounts the same `NoteId` at its new revision, preventing a later save from using a stale session. Core mutation tests cover ID-preserving rename/reopen, move/tag ordered relations, delete/rehome, and a real SQLite authorizer rejecting body/blob reads during active metadata reconciliation. New app/mounted tests cover a committed-refresh failure (durable relation present but every live field unchanged), correct session remount, actual panel rename/delete/create/move/tag, Trash restore/purge, and visible action error with no session change. Evidence crosswalk: `task-6-stage-c-report.md`. Initial verification: core default 88/88, core test-support 122/122, app target 76/76, UI 58/58, full GPUI exact-donor-skip 1,179/0/1, check/fmt/diff PASS. The initial checkpoint omitted confirmation workflow; the review-remediation entry below supersedes that limitation. Task 6/Release remained incomplete.

Task 6 Stage C1 review remediation (2026-09-12, shared dirty worktree, uncommitted): the review blockers are now addressed without adding a second editor/resource authority. Trash mounts the existing `NoteSession`/surface read-only, keeps selection/scroll/copy and Restore/Purge, but rejects title/body/IME/paste/drop/picker/format mutations; Restore bypasses a stale failed read-only flush. Organization events reconcile through one complete candidate packet and same-`NoteId` revision remount, so a committed warning cannot be cleared by a same-card click. Stack destroy/disband preserves child Notebook/Note IDs; navigation history is sanitized against tombstoned Notebook/Tag/Stack routes. Notebook/Tag/Stack deletion and permanent purge use typed two-step confirmation with stale-target fencing; Trash hides impossible Move/Tag and empty recovery controls. Permanent purge is occurrence-aware and has a v6 durable `resource_gc_queue`, so unique metadata/blob cleanup is retried after safe physical unlink failure while shared resources survive. A normal title/body snapshot now hands its complete transaction-built `Note` back to `AppModel` before later organization candidates reuse active session data; the production observer test locks metadata-only reconciliation, and the mounted test locks dirty-save → organization remount → later 100/500ms save → reopen. Current verification: core default and test-support full suites PASS; organization core 9/9; GPUI app 81/81; UI 66/66; full exact donor skip 1,192/0/1 PASS. Stage C remains uncommitted, requires independent re-review and does not mark Task 6/Release complete.

Task 6 Stage C1 nested-Notebook create Release remediation (2026-09-12, shared dirty worktree, uncommitted): the real sidebar route → New Note repro is now source-backed by Evernote `74300__module-74300.js::createNewNote/createNewNoteExtended`, whose typed `CREATE_NEW_NOTE` payload carries an explicit `container` rather than inferring owner from a filtered list. `AppModel::resolve_create_note_destination` compiles the current typed route before the existing repository transaction: Notebook uses its durable ID; Stack uses the selected child Note's Notebook or exactly one child Notebook, while zero/multiple children visibly require a target; Tags and Trash safely use All Notes/default-container fallback. `PendingReconciliation::CreateNote` retains the typed destination route, so an initial candidate failure cannot recover a valid Notebook create under All Notes. RED→GREEN AppModel tests prove the old fixed `notebook_id: None` wrote Default instead of the selected nested Notebook; a new isolated mounted test clicks actual sidebar `In stack notebook` then `library-create-note` and asserts SQLite owner, route, selection and active session all agree. Verification: core default/test-support PASS; GPUI app 94/94; UI 77/77; full exact donor skip 1,218/0/1; no commit/tag/push. This repair does not mark Stage C, Task 6, or Release complete.

Task 6 Stage C1 Release quit remediation (2026-09-12, shared dirty worktree, uncommitted): the visible Library menu formerly dispatched its private `QuitLibrary` action while macOS Cmd-Q dispatched shared `QuitApplication`, leaving Cmd-Q without a Library save handler. A dirty private-quit request also started the retained snapshot then returned permanently, requiring an unsafe/useless second click. The production menu and shared key binding now use one `QuitApplication` handler; `LibraryMenuLifecycle` retains one pending request until every `LibraryShell` has completed its existing exact-generation flush, then issues an idempotent platform quit. Native Release tracing then found the actual remaining data-loss path: GPUI runs a menu action while the same window is temporarily removed from its window map, so a nested `shell.update` errors; the former `unwrap_or(Ready)` immediately quit. `request_quit_library` now defers the retained flush to the end of the effect cycle, and an update error is fail-closed `Blocked`. Evernote `62264__application-controller.js::initBrokerSubscribersPreConduit` (`boron.actions.quit` → `this.quit`) is the source evidence for the single application command; GPUI defer/error handling is explicitly a local product design. Mutation-sensitive tests include a true `VisualTestContext::dispatch_action` window-stack regression: delete `cx.defer` or turn the error back into Ready and the durable-body assertion REDs. Current directed verification: menu 5/5, quit lifecycle 3/3, `cargo check --tests` PASS. Fresh Release hands-on is now positive for this blocker: in `/tmp/joplin-lite-quit-final-profile.3BHk7G`, menu and Cmd-Q each received a new Cmd-V committed suffix while SQLite still held the previous revision, waited for exact snapshot, then exited with revisions 3→4 and 4→5 and both suffixes durable; a Rime marked composition correctly blocks exit instead of losing it. This closes only the menu/Cmd-Q Release regression, not Task 6/Stage/Release completion.

Task 6 Stage C1 Quit safety and organization-input closure (2026-09-12, shared dirty worktree, uncommitted): review found two remaining fail-open boundaries and a form regression. `flush_library_windows_for_quit` now allows only the explicit no-session `StartupErrorView`; unknown live roots and every shell/window update error are visible, fail-closed `Blocked` states. The close guard also maps an unavailable weak shell update to a critical prompt plus `false`, never a successful close. New real mounted coverage directly invokes production flush inside the same `VisualTestContext::update` window stack; mutating the `shell.update` error arm to `Ready` makes it RED. Additional lifecycle coverage exercises IME composition, two dirty LibraryShell windows, a gated background worker and repeated Quit requests. Directed results: library-menu 8/8, quit-lifecycle 7/7, GPUI `cargo check --tests` PASS. Separately, all three organization creation buttons and the same field's Enter route now pass through `LibraryShell::submit_organization_create`, which clears the shared `TitleInput` only after `AppModel::dispatch` returns success; invalid whitespace create keeps the exact title for correction. New real input/button tests are 2/2 PASS; deleting the clear or making it unconditional makes their success/failure assertions RED. Final local verification: core default/test-support suites PASS, GPUI exact donor-skip 1,232/0/1, test-check, release build, both crate fmt checks and `git diff --check` PASS. Evidence crosswalk is in `task-6-stage-c-report.md`. Fresh-profile Release handoff and independent re-review remain pending; Task 6/Stage/Release are not marked complete.

Task 7 source-first checkpoint (2026-09-12): before search implementation, the controller re-read reconstructed Evernote main modules `56535` (local saved searches/suggestions separated from remote semantic methods), `83028` (AST-to-SQL note search, FTS union and BM25 ranking), `08905` (typed suggestion filters/history), `12876`/`36175`/`41774` (FTS tables, trigger maintenance, bounded history and indexing state), `48353`/`59009`/`98189` (attachment metadata/extracted-text indexing), Boron renderer Search CSS/control flow in chunk `9435`, and original editor TypeScript from `common-editor.ce.js.map` for `find.ts`, `findnext.ts`, `findprev.ts`, `state.ts`, `utils/index.ts`, and the shell-owned Find-in-Note overlay. The frozen crosswalk in `task-7-source-brief.md` keeps local library search separate from Cmd-F literal find, marks dual unicode61+trigram Chinese substring search as an independent product improvement, excludes AI/network fallback, and requires bounded paging/decorations plus attachment OCR/PDF indexing that cannot block local save.

Task 6 Stage C1 Cards thumbnail checkpoint (2026-09-12, shared dirty worktree, uncommitted): Cards now renders a real selected JPEG thumbnail rather than the former status text, while preserving the one lightweight projection authority. `note_card.rs` only consumes `selected_thumbnail_id` and an asynchronously adopted source; `card_thumbnail.rs` owns a separate 4 MiB/192px list cache, a single descriptor-safe background reader and task-private source lease, so it never hydrates note body/blob data or touches the active editor's image residency. The GPUI UniformList runtime measures index zero before the actual viewport, so `LibraryShell` records ranges and commits only the final range at the end of the effect cycle; this prevents a tail scroll from repeatedly materializing index zero. Real mounted tests cover JPEG draw/no body-or-blob read, text-only Cards with no empty tile, 192-card bounded desired/queue residency, tail measurement exclusion, and Cards-exit lease/cache release; `note_card_tests` 4/4, collector tests 3/3 and the existing 1,662-tail gate all PASS. Exact Evernote source mapping and the explicit independent streaming/cache architecture are recorded in `task-6-stage-c-report.md`. This does not close the Release smoke or Task 6 completion gates.

Task 5 M1 real Release gate (2026-09-12, after the earlier stale-binary rejection): PASS on a new `/tmp/joplin-lite-m1-accept.xhGd3fks/profile` with actual staged executable SHA-256 `864df20c71d8cc6a7933854db8133ec7539c3c722ed92c52c24e77b1a094c2db`. In the fresh profile created exactly three Chinese-title/body notes; pasted a real app screenshot from clipboard and another picture, edited before/after image, inserted PDF, dragged a 1218×2494 JPEG from Finder with actual drag events, switched rapidly, exited via Cmd-Q, reopened and reselected all three, then exited again. Images/cards were visible immediately without note-switch workaround, with titles/body/resources persistent. SQLite after exit: integrity `ok`, no foreign-key violations, 3 active notes, 4 resource rows and ordered note-resource relations, all on-disk blob hashes matched. Full report and screenshot names: `task-5-m1-release-acceptance-2026-09-12.md`. The Task 5 code is already committed at `90e4fda54`; this tested Release also contains uncommitted later Task 6 C1 changes, so it is **not** assigned a misleading tag or pushed as that older commit. M1 functional outcome is accepted; Task 6 Snippets density, organization scale/motion and Tasks 7–10 are not.

Task 6 C1 Snippets and C2 navigation incremental gates (2026-09-12, uncommitted): Snippets now divides each 88pt row into bounded title/preview/date; independent review 0C/0I/0M and 1160×789 copied-profile Release screenshot verified three visible dates with no adjacent-row overlap (`task-6-stage-c-report.md`). The real sidebar bug clearing selection when moving a note into its matching notebook is repaired with typed NoteId carry and final projection authority. The first C2 patch was rejected for a dirty-session TOCTOU; all NavigateTo now cross the existing NoteSwitch flush barrier before a candidate can unmount, with a deterministic preflight→external move→query RED/GREEN test. Independent re-review: APPROVED 0C/0I/0M, mounted navigation 7/7 and AppModel 97/97. Copied-profile Release replay confirmed Notebook/Tag retain the same note, excluded route clears stale editor, Cmd-Q exit 0, SQLite integrity `ok` (`task-6-c2-navigation-release-smoke-2026-09-12.md`). None of this marks overall Task 6 done: default 1160px toolbar clips `组织`, scale/perf and pane motion remain, and Tasks 7–10 remain incomplete.

Task 6 C2 compact toolbar gate (2026-09-12, uncommitted): the prior default-window `组织` clipping is now repaired by an editor-column-aware compact library toolbar and anchored More for the existing View/Sort/Trash actions. Mounted toolbar tests 4/4 and GPUI donor-skip 1,268/0/1 passed; independent reviewer reported 0C/0I/1M, with manual Release replay subsequently covering actual New Notebook button and widths 1180–1220 on both sides of the 640px editor-column breakpoint. Fresh isolated 1160×789 GUI had all primary buttons visible, created a notebook and a note, switched View Card→Snippet through More; Cmd-Q exited 0 and SQLite integrity/foreign-key/body persistence checks passed. SHA-256 `1574b88d1e881192cf9e3da974829f9e5189dd9dc716089d0b5f9f5320ff202a`; evidence `task-6-c2-toolbar-release-smoke-2026-09-12.md`. Task 6 scale/perf and pane motion, plus Tasks 7–10, are still open; no Task 6 commit/tag/push.

Task 6 C1 scale/query boundary (2026-09-12, uncommitted): the first 1,662 notes/31 notebooks/64 tags/4,238 resources test was rejected in independent review (0C/2I) because only 1,661 attachments were related and it did not exercise full-fixture tail/filter/panes. The corrected fixture relates all 4,238 resources to canonical image blocks and checks 1,662 HTML/revision/preview/thumbnail snapshots. Two full mounted tests now check three list modes' tail ranges, notebook/tag filters, 3/2/1 pane transitions, SQL no body/blob read, one selected NoteSession and bounded visible thumbnail work. Old relation count and detached scroll-handle mutations were actually RED; corrected tests each independently PASS 1/1, full donor-skip 1,270/0/1, re-review APPROVED 0C/0I/0M. Evidence `task-6-c1-scale-performance-report.md`. No production code changed in this specific cut; this is **not** a measured Release RSS/latency pass, pane animation or overall Task 6 completion; no commit/tag/push.

Task 6 C3 pane motion assessment (2026-09-12): **BLOCKED / NEEDS_CONTEXT; no source implementation added.** Direct mounted RED showed `ToggleNoteList` enters the current 0px `render_note_list(!visible)` path in the same paint, so the actual Cards `UniformList` processor is gone before a pre-settled frame. GPUI 0.2.2 `with_animation` requests layout on each animation frame; applying it to the flex pane widths would continuously resize/re-layout the retained editor and feed intermediate Cards viewports, violating the C3 final-range/final-relayout budget. A potential absolute clipped-overlay compositor design needs dedicated hit-test, reversal, resize, final thumbnail reconciliation, and retained-entity proofs before it can replace the stable instantaneous fallback. Full source map, RED result, and next-proof matrix: `task-6-c3-pane-motion-report.md`.

Task 7 Stage A core search (2026-09-12, shared dirty worktree, uncommitted): schema v7 adds disposable local `unicode61` and trigram FTS projections; all authoritative save/trash/restore/purge paths retain the pre-existing atomic `search_queue` replacement. A bounded 100-job worker replaces/removes title/body projections and conditionally acks full queue identity, so a leased old job cannot consume a newer snapshot. Typed parser/query supports quoted/escaped phrases, negation, unknown-operator-as-text, notebook/stack/tag-AND/date/trash/attachment/filename/MIME filters, SQL page cap 500, title/body query, and `SearchHit` projection without canonical-body/blob hydration. One/two-CJK-character lookup uses parameterized derived-index `LIKE` fallback; 3+ CJK uses trigram MATCH; this dual strategy is an independent product decision, not claimed as exact Evernote source. RED missing-API proof then GREEN: search 5/5, migration 20/20, full core feature suite 124 integration/unit plus 56 lib tests, 0 failures, diff check 0. UI route/Cmd-K/Cmd-F, history/suggestions, OCR/PDF, resource extracted text, worker scheduling/status and M2 profile performance are explicitly not complete. Crosswalk/risks: `task-7-stage-a-core-report.md`.

Task 7 Stage A round 1 repair (2026-09-12, shared dirty worktree, uncommitted): parser preserves negative/quoted field tokens, invalid/descending date ranges remain ordinary text, and typed `Not(Tag|Notebook)` filters compile as negative SQL predicates. Latin terms/phrases now use unicode61 only (so `cat` cannot hit `education`); CJK/mixed takes trigram semantics and short CJK makes one mapped derived-table candidate scan. Schema v8 adds explicit stable `search_index_rows.fts_rowid` mapping; worker FTS replace/delete uses this rowid rather than notes implicit rowid, and SearchHit uses the same thumbnail metadata fallback as list projections. Directed RED→GREEN: search 6/6, migration 20/20, fmt/diff clean. Outstanding: explicit v6 *pending* job preservation fixture, thumbnail equivalence fixture, worker error status, full scale/explain evidence, and all later Task 7 stages.

Task 7 Stage A round 2 closure (2026-09-12, shared dirty worktree, uncommitted): v6 migration fixture now retains and observes an existing `pending-before-v7` job; associated-image fixture proves SearchHit/ListQuery select the same thumbnail. All FTS query branches now use one non-correlated derived candidate set joined through explicit `search_index_rows.fts_rowid`; purge removes the empty map after deleting FTS rows. Search 7/7, migration 20/20 and complete core test-support suite PASS; fmt/diff clean. The remaining explicit limitation is a committed 1,662-note EXPLAIN/latency fixture plus worker classified error state, not falsely marked complete.

Task 7 Stage A round 3 independent gate (2026-09-12, shared dirty worktree, uncommitted): round-1 independent review found 5 Important; round-2 scoped review verified all five closed but found a new Important because parser created `Not` for every filter while SQL handled only Tag/Notebook. Round-3 restricts typed negative filters to Tag/Notebook; other negated field-looking tokens safely remain ordinary negated search text rather than raising `InvalidSnapshot`. Real repository regression covers six fallback inputs and the default Trash boundary. The final scoped independent reviewer marked this fix **APPROVED, 0 new Critical/Important** and independently ran the focused fallback test 1/1. Controller then freshly reran search 8/8, complete core test-support 56 lib + 81 integration = 137/137, fmt and diff checks, all exit 0; the earlier implementer report's 124 integration count was inaccurate and has been corrected in the report. Current schema is v8, not v7. This accepts only the Task 7 Stage A core cut—not the whole Task 7 or M2. Negative Stack/date/attachment/filename/MIME/Trash field-filter semantics, background scheduling/status, Search UI/Cmd-K/Cmd-F, history/suggestions, attachment OCR/PDF, bundled/1,662 Release latency/memory remain open. No commit/tag/push or personal-library migration.

Task 6/7 interim snapshot (2026-09-12): user requested tag/push of the existing implementation, then continuation. Fresh core default/test-support suites, GPUI exact donor-skip 1,270/0/1, both crate fmt/test-check, Release build and staged diff check passed. Commit `247f385521c26fe6967e7e4f3a07e4433e47bdca` and annotated `joplin-lite-native-v0.14.0-organization-search-core-snapshot` were pushed to `origin` and remote dereference verified at that commit. This is an organization/search-core **snapshot**, not Task 6/7 completion or M2 acceptance; see `docs/research/evernote-11.32.5-organization-search-snapshot.md`. Worktree was clean after push. Next Stage B1 owns the live offline indexing scheduler; its brief is `task-7-stage-b1-brief.md`.

Task 7 Stage B1 first implementation (2026-09-12): Terra commit `7d54daa7680c0cded9846677073b854db06896fa`, tests reported core + GPUI 1,274/0/1 and Release build pass. Task review **CHANGES REQUESTED**: 1 Critical (synchronous 100-note SQLite/FTS batch runs on GPUI ForegroundExecutor, violating input/navigation/Cmd-Q nonblocking), 2 Important (pending indexing status invisible; failed-index notice overlaps resource notice), 1 Minor (mounted save and active-batch cancellation coverage). Review: `task-7-stage-b1-review.md`. Fix round 1 sent to the same Terra implementer; no Stage B1 acceptance, tag or push yet.

Task 7 Stage B1 fix round 1 (2026-09-12): Terra commit `daf6ed0c552fc303028d0c49370ec455e0cf7c4c`, reported core 129 and GPUI 1,276/0/1 plus Release build. Scoped re-review closed the GPUI foreground batch, Pending visibility, and current failure/resource notice overlap, but found one new load-bearing Important: the worker still holds the same repository SQLite connection mutex as foreground note load/save, and the mounted barrier stops before that mutex/transaction. Active-batch cancellation/save coverage remains unproven. Fix round 2 requests a profile-bound separate index connection and a deterministic contention test. Stage B1 is **not accepted**, and no additional tag/push was made.

Task 7 Stage B1 fix rounds 2–3 (2026-09-12): Terra commits `9cb596e59` and `8c4f97933` put search indexing on a separately verified, profile/inode-bound WAL handle and added note-boundary cancellation to the real mounted shell scheduler. The independent reviewer found no remaining Critical/Important code blocker at round 3: foreground note load/switch proceeds during a real active index transaction; closing the mounted shell lets only the current atomic note finish and preserves two later queue identities. The reviewer correctly amended the earlier concern that saving was synchronous in GPUI: NoteSession journal/snapshot writes already use a background executor. SQLite single-writer contention can still delay a background save/dirty switch/Cmd-Q on a very large note or slow disk, so latency and exact UI save state remain a Minor/performance acceptance item, not a claimed zero-wait guarantee. Controller independently reran core `--features test-support` PASS, GPUI exact donor skip **1,278 passed / 0 failed / 1 filtered**, both crate fmt checks, diff check and GPUI Release build PASS at `8c4f97933`. The current Release executable opened an isolated empty profile and created its SQLite/WAL/resource structure; native screen automation transport closed, so visual/interactive Release acceptance is **not** claimed. B1 code slice may supply Stage B2; Task 7/M2 remain incomplete. No further tag/push/deploy or personal library access.

Task 7 Stage B2 review cap (2026-09-13): after the tagged/pushed `247f385` snapshot, local commits `9101736ec..6763004e8` added typed SearchRoute history, visible retained Cmd-K palette, bounded background local FTS, durable queue/generation refresh fencing, session-switch barriers and indexed refresh. Five scoped independent reviews culminated in `task-7-stage-b2-review.md` Round 5: **B2 NOT ACCEPTED**, with four real Important findings. Controller independently ran core `cargo test --features test-support` PASS and GPUI exact donor-skip **1,290 passed / 0 failed / 1 filtered**; `git diff --check` and GPUI fmt passed, but core fmt exposed an unformatted `has_pending_search_jobs` expression. The 00:46 Release executable visibly opened a disposable `/tmp/joplin-lite-b2-smoke.QmrtjU` profile, displayed Cmd-K/empty results, closed with Escape and exited via Cmd-Q with code 0; this binary predates the last B2 commits and the AppleScript-driven CJK candidate/Return sequence was inconclusive, so final HEAD visual/IME acceptance is not claimed. No post-snapshot tag or push.

Task 7 Stage B2 breaker rulings (2026-09-13): (1) Dirty-at-query-start → Clean-after-autosave with unchanged `save_generation` can admit a pre-save search packet: **real and load-bearing**; next bounded repair must fence actual save completion/revision and preserve editor/undo/selection. (2) `index * 58px` keyboard scroll estimate diverges from variable-height result rows: **real and load-bearing for Cmd-K keyboard navigation**; use actual row geometry or a truly fixed-height list with a mounted distant-row visibility proof. (3) `commit_search_refresh` errors and pending/no-event retry lack visible recoverability: **real and load-bearing**; keep the old coherent packet, show the exact error and an explicit retry path. (4) Focus capture omits some real controls and can close the organization panel before restoring focus: **real, deferred only as to exhaustive rare controls, but the next repair must cover every currently focusable search-entry context before B2 acceptance**. None is silently parked as a completed feature. Carry all four into a fresh Task 7 SearchRoute safety/interaction repair before suggestions, Cmd-F, OCR/PDF or personal-library migration; B2 remains partial and Task 7/M2 incomplete.

Task 7 Stage B2c review round 1 (2026-09-13): Terra commits `9c9116713`, `b72f58723`, `332e77002` add a durable-revision save fence, actual GPUI scroll geometry, generic focus capture, explicit search Retry, mounted 500 real-result visibility/tail click, and deterministic same-generation autosave stale-packet and refresh Err→Retry click tests. Independent reviewer: **NOT ACCEPTED**, 0 Critical / 3 Important / 2 Minor. Important issues are history `Ok(false)` without rescheduling, Retry that may become stuck when its destination disappears, and proof/visibility for non-organization focus overlays. All enter B2c fix round 1; no B2 acceptance or new push/tag. Controller independently reran final-head core suite, GPUI 1294/0/1, both fmt and diff check PASS. Final Release smoke still pending.

Task 7 Stage B2c fix round 1 and bounded acceptance (2026-09-13): Terra commits `37dd66571`, `db079032d`, `5ea454258` closed all three Important findings; scoped independent re-review at `a2559a14c` found **0 remaining Critical/Important**. Mounted regressions cover same-generation autosave fencing, real 500-row scroll/tail click, refresh failure→Retry click, history persist failure→Forward Retry click, and Link-popover focus/visibility after Search Escape and backdrop click. Controller independently ran core `cargo test --features test-support` PASS, GPUI exact donor skip **1,296 passed / 0 failed / 1 filtered**, both crate fmt checks and diff check PASS, then final-HEAD GPUI Release build PASS. A disposable `/tmp/joplin-lite-b2c-final.3kXVVp` Release launch visibly passed empty-library startup, Cmd-K palette, Escape, Cmd-N note creation and Cmd-Q exit 0; SQLite integrity `ok`, one note. Evidence: `task-7-stage-b2c-review.md`, `task-7-stage-b2c-release-smoke-2026-09-13.md`. This accepts only the bounded B2c code/empty-profile interaction slice, **not** all Stage B2, Task 7 or M2: populated result/IME Release use, suggestions, Cmd-F, OCR/PDF, 1,662-note performance/RSS, and personal-library migration remain open. Review Minors (stale `Ok(false)` explicit injection, Back/vanished-target Retry, OS-thread test-gate panic cleanup, Up/wheel scroll) carry to later full-branch review. No post-snapshot push/tag/deploy.

Task 7 Stage C1 Task 1 find engine (2026-09-13): Terra commits `d8297b14b`, `0ce7c5f99`, `d5c53f479` add ephemeral literal in-note `FindState` (per text-block revision cache, UTF-8 ranges, circular primary), visible-only match paint, retained two-stage offscreen reveal, and 64-hard-line sparse layout checkpoints with bounded find-only soft/hard-row geometry. Original Evernote source from `common-editor.ce.js.map` (`find/state.ts`, `find/plugin.ts`, `find/commands`, `find/ui`) was re-read and mapped in `docs/superpowers/plans/2026-09-13-find-in-note-c1.md` and `task-7-stage-c1-task1-report.md`. Independent review found and closed dense single-paragraph 4,800-geometry, O(N²) structural cache retention, and idle/tail hard-line paint scans; final gate **APPROVED, 0 Critical/Important**, with two deferred Minors documented in `task-7-stage-c1-task1-review.md`. Implementer reported focused 10/10, GPUI exact donor skip **1,306/0/1**, fmt/diff and Release build PASS. Controller independently reran focused 10/10 and both crate fmt/diff PASS. This accepts the local find *engine only*: Cmd-F UI and disposable Release smoke are Stage C1 Task 2, and Task 7/M2 remain incomplete. No post-snapshot push/tag or personal-library access.

Task 7 Stage C1 Task 2 bounded acceptance (2026-09-13): Terra commits `40786e6cd..ce2efae55` add the retained native Cmd-F panel, Cmd-G/Shift-Cmd-G, case toggle, narrow-panel horizontal viewport/IME bounds, 10,000-match count, and exclusive Cmd-K handoff with original focus restoration. Each step mapped back to unpacked original Evernote find UI/keybinding/state/command sources in `task-7-stage-c1-task2-report.md`; independent review went from 3 Important to **0 Critical / 0 Important** after three remediation rounds (`task-7-stage-c1-task2-review.md`). Controller independently ran final HEAD GPUI **1,308/0/1**, both crate fmt and diff check PASS, Release build PASS, then launched final binary SHA `13451d9a6725af4e2dda83c43af114ccbdfb3a1602878137669ad2a7ef2aa54c` on disposable `/tmp/joplin-lite-c1-final.NxZQsF`: empty start, Cmd-N, Cmd-F, Cmd-K→Cmd-F, Escape, Cmd-Q exit 0, SQLite integrity `ok`, one note. See `task-7-stage-c1-final-smoke-2026-09-13.md`. Squirrel/Rime intercepted AppleScript ASCII as provisional composition, so populated Release find/navigation and physical Chinese IME are **not claimed**; mounted committed-CJK/marked tests pass. This accepts bounded C1 code/UI only, **not** Task 7/M2, suggestions/history, OCR/PDF extraction, 1,662-note perf/RSS, or personal-library migration. The only remote tag/branch remains the prior `247f385` v0.14.0 organization-search-core snapshot; no C1 tag/push/deploy.

Task 7 Stage D1 bounded resource-filter acceptance (2026-09-13): Terra commits `577ab4c8c`, `339b3f1ea` make `filename:`/`mime:` filter only current associated, non-deleted resources and return deterministic matched `ResourceId`; `hasattachment:` and result count now use that same live-resource boundary. Original Evernote `83028` resource operator SQL and `48353` attachment metadata FTS migration were re-read; the native query retains its pre-existing contains matching, explicitly differing from Evernote's exact/prefix operator semantics. Independent review's one Important consistency gap was fixed; final re-review reported **0 Critical/Important**. Controller independently ran search **11/11**, full core **140/140**, fmt and staged diff check PASS. See `task-7-stage-d1-resource-filter-report.md`. General-term filename FTS, OCR/PDF/extracted text, populated Release UI, 1,662-note performance/RSS and personal-library migration remain open; D1 is not Task 7/M2 acceptance.

Task 7 Stage D2 bounded filename-term acceptance (2026-09-13): Terra commits `bc8cedbfe`, `a73dcc3d8`, `b05d49690` add a v9 resource-owned unicode/trigram filename FTS projection with transactional insert/update/soft-delete/hard-delete triggers, live-relation ordinary-term union, and FTS-consistent deterministic `matched_resource`. v8→v9 rebuild reads only live resource metadata and does not rewrite note bodies. This is an explicit native usability enhancement: Evernote `83028::k` general-term union names extracted attachment `searchText`, while `48353` separately maintains metadata FTS. Independent review found **0 Critical/Important** after two added focused cases; one Minor public-purge filename-query test remains deferred to M2. Controller independently ran core **149/149**, GPUI **1,308/0/1 exact donor filtered**, both fmt checks, diff check and Release build PASS (SHA `b166f5e8fed353a94d503be99fe249feba7b8cab140ecfbf9ed8f6728e3be602`). See `task-7-stage-d2-filename-terms-report.md`. Populated Release search, 1,662-note latency/RSS, PDF text, image OCR and personal-library migration remain open; Task 7/M2 is not accepted.

Task 7 Stage D3a bounded derived-text foundation acceptance (2026-09-13): Terra commits `c2191214a` and `067397088` add v10 per-resource disposable extracted-text FTS, bounded pending/indexed/failed jobs, exact ResourceId+SHA+extractor-version publish and live-association gates, search union/provenance, and migration queueing without blob/body reads. The first independent review's one Important (no v10 requeue on extractor upgrade) was closed by a single version constant plus a settings sentinel that keeps ordinary reopen read-only and reconciles live image/PDF only when version changes. Final review: **0 Critical / 0 Important**. Controller's first `test-support` run exposed two stale `user_version=9` test assertions, fixed in `067397088`; final full `test-support` **155/155**, GPUI **1,308/0/1 exact donor filtered**, both fmt checks, diff check and Release build PASS (SHA `58a2e5085138851164eeb5fae8f3f5ecfcc44e533fb75cc9b4eacf8e615ce4a5`). See `task-7-stage-d3a-derived-text-report.md`. This is injected-text infrastructure **only**: no real PDFKit/Vision extraction, OCR UI, populated Release search or M2 acceptance. Short CJK large-text latency/RSS remains to measure.
