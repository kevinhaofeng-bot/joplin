# Joplin 现用资料库：只读元数据资格盘点（2026-09-13）

本页只是对当前 Joplin Desktop SQLite 做 `mode=ro` 聚合查询；没有导出 JEX、读取笔记正文或标题、复制/修改资料库，也不构成迁移验收。资料库仍由 Joplin 使用，数字可能继续变化。JEX 的实际字段和资源文件必须以独立导出的副本重新验证。开源 Joplin `InteropService.ts::export` 从各 folder 的 `noteIds` 选择笔记，按正文链接选资源，并只导出所选笔记的 note-tag/tag；`Folder.notes` 默认排除 `deleted_time != 0`。因此 SQLite 总量与普通 JEX 的数量**不能直接相等比较**，特别是回收站和未被正文引用的资源。

| 只读聚合项 | 当前数量 | 对迁移的含义 |
| --- | ---: | --- |
| 笔记 / 文件夹 / 标签 | 1,669 / 31 / 64 | 真实规模，不沿用旧计划中的 1,662 篇数字 |
| 笔记—标签关系 / 资源 / 笔记—资源关系 | 773 / 4,238 / 4,463 | C2c-3c-2 的合成关联测试之后仍需按实际 JEX 对账 |
| Markdown / HTML 笔记 | 1,669 / 0 | 这是当前 SQLite 的 `markup_language`，不是已验证的 JEX 输出 |
| 待办笔记 / 有截止时间 / 有完成时间 | 40 / 16 / 38 | 当前 JEX stage 对 `is_todo` 非零明确阻断，不能视为可迁 |
| 已删除笔记 / 冲突笔记 | 4 / 0 | 活跃与回收站数量需要分别核对，不得只比总数 |
| 有 `source_url` / 非零自定义排序 `order` | 426 / 1,474 | 当前 stage 的 note metadata 白名单不接受这些字段；不能静默丢原网页来源或排序 |
| 嵌套文件夹 / 最大层级 | 14 / 2 | 当前两级文件夹模型覆盖深度，但不覆盖所有父子混合形态 |
| 同时有自有笔记和子文件夹的根文件夹 / 其直属笔记 | 1 / 187 | C2c-3c-1 明确阻断，后续需提供可见、可审计的映射，而不是压平或漏掉 187 篇 |
| 未归档到文件夹的笔记 / 孤立文件夹 / 重复 note-tag 目标对 | 0 / 0 / 0 | 仅说明当前 SQLite 聚合，仍须在 JEX 副本中复核 |
| 常见资源 MIME | JPEG 3,178；PNG 542；GIF 208；PDF 141；DOCX 78；旧 DOC 21；WebP 16 | 当前 C2c-3b 仅支持受限 PNG/JPEG/PDF/TXT/BIN 等；多模态历史资料不能按“资源总量已能迁”报告 |

另一个直接从源码可见的兼容边界：`BaseItem.ts::serialize` 默认逐个写出模型的 `fieldNames()`，包括值为空或零的字段；现有 Rust JEX stage 只接受窄字段白名单。因此合成小样本能入库，**不能推断当前 Joplin 导出的真实 JEX 可直接入库**。后续须对真实 JEX 副本统计每类字段/正文/资源的 blocker，再把可保真的字段逐项映射或保留为明确可恢复的审计元数据。

已找到独立于现用 SQLite 的既有备份 `/Users/kevinhao/JoplinBackup/default/all_notebooks.jex`，修改时间 2026-09-12 23:10:25，本次只读 `file` 确认为 POSIX tar，tar 路径列表有 6,338 个 `.md` 元数据条目与 4,153 个 `resources/` 条目；SHA-256 为 `1d0feb4fe025379db3a869bc348d0769de4b9b23b3a752580d0eb522c607d6cd`。外层条目数不能直接等同于上述现用 SQLite 实体总量；资格检查的具体覆盖范围和阻断见下段。原备份未被修改。

## C2c-4a 对现成 JEX 备份的首次只读运行

在提交 `90327835c` 的资格检查器通过独立复审、控制者复跑 core 212/212 后，以显式空临时父目录运行只读 CLI。此快照共有笔记 1,665、文件夹 31、资源元数据/物理文件各 4,153、标签 64、note-tag 关联 425；与当前 SQLite 相比，普通 JEX 不导出 4 篇已删除笔记。SQLite 的 773 条 note-tag 中，425 条指向现存未删除笔记，另 348 条指向不存在的 note ID；后者也不在 JEX 中。这里是两份不同时间状态的资料，不能把差额都算成迁移器错误。

结果 `semantic_scan_completed=false`、`ready_for_current_stage=false`：预检定位 1 篇笔记正文里的未导出内部引用（source ID `17e247891c1c48a2ae1b826b7917b3a0`），以及 1 张 `14,050,700` 字节 PNG（source ID `3dc7ddcbc035fb864c41178ed8549b08`）超出当前图片 Store 的 10 MiB 限额。并未进入逐笔记/逐字段语义资格扫描，故不能由这次运行推断其余 1,664 篇或 4,152 个资源可迁。运行约 33.65 秒，`time -l` 最大 RSS `23,035,904` 字节；临时子目录自动清除，显式空父目录已删除。原 JEX 前后 SHA-256 同上。下一切需要独立只读路径在保留这两项预检阻断的同时继续汇总其余语义问题，不能改写原备份或放宽已验收的暂存入口。

## C2c-4b 对同一备份的完整只读语义资格检查

提交 `487211f6e` 的独立资格检查器通过复审（0 Critical / 0 Important / 0 Minor），控制者在该 HEAD 重跑 core `cargo test --tests --quiet` 共 217 项、CLI 示例测试 1 项、fmt/diff 均通过后，再用同一 JEX 与新建空临时父目录运行。第二遍按条目重新核对路径、ID、长度和 SHA-256；资源只分块读取，不建正式资料库。这次 `semantic_scan_completed=true`，但 `ready_for_current_stage=false`；完整扫描不等于可导入。

实体总数仍为 1,665 笔记、31 文件夹、4,153 资源、64 标签、425 关联。分类器保留 1 项未导出内部引用（关联目标 ID `d774eda2fdf10b3b6546596b1e65c86c`）与 1 项超当前图片 Store 上限的 PNG。后续语义分布如下；各类别可以落在同一实体上，**不能相加为受影响笔记数**：

| 资格类别 | 涉及实体 / 发现次数 | 含义 |
| --- | ---: | --- |
| `ExporterFieldGap` | 2,185 / 32,403 | Joplin 导出器的完整字段集合与当前窄 stage 字段白名单不一致；其中空值/默认值也会被导出，不能把次数直接当作有价值内容损失 |
| `UnmappedSemanticField` | 4,610 / 13,622 | 非默认字段尚无产品保真映射；需按字段分布决定可见属性、审计元数据或明确阻断 |
| `StageValidation` | 3,878 / 8,631 | 现有严格暂存入口的元数据、资源类型和其它条件不满足；不是已尝试导入失败的计数 |
| `BodyFidelity` | 490 / 490 | 490 篇正文不能由当前受限转换器安全投影，必须逐类补转换或保留可读原文并提示 |
| `FolderHierarchy` | 1 / 1 | 一处父文件夹同时有直属笔记和子文件夹，当前映射会阻断 |

资源类型以 JPEG 3,169、PNG 476、GIF 204、PDF 140、DOCX 78、DOC 21、WebP 16 为主；这是**JEX 备份**分布，与现用 SQLite 的资源数量不必相等。扫描用时 78.19 秒，`time -l` 最大 RSS `56,000,512` 字节（约 53.4 MiB）；这是资格检查命令行进程，不是常驻 GUI 内存。临时子目录自动清空，显式空父目录已删除；原备份前后 SHA-256 仍为 `1d0feb4fe025379db3a869bc348d0769de4b9b23b3a752580d0eb522c607d6cd`。

下一步按这个实测分布推进**保真导入能力**，先化解导出器默认字段与大量资源的假性/真实阻断，再处理非默认来源、排序、待办、混合文件夹和正文格式。必须继续保留原始审计字节、不给不支持内容假成功；正式资料库切换、可读导出/恢复和真实可用验收仍未完成。

## C2c-5a 精确默认值兼容后的只读复扫

在 `33d48898f`、`0c6c9f6ea`、`ce7403188` 中，严格隔离 stage 仅对有 Joplin 源码证据的逐字段默认值放行；`Note.filter` 的零坐标按 `0.00000000`／`0.0000`，而非合成测试最初写的 `0`。非默认字符串及伪装字段名不再通过解析器的 trim 冒充默认值。原两项 Important 经独立限界复审关闭，余 2 Minor：同一实体同时有非规范语法和字段 gap 时资格报告不额外列一条 StageValidation（仍阻断）；新增完整默认值组织 golden 的自身重开断言少于旧组织测试。这不构成正式导入验收。

对同一 SHA 的 JEX 使用 `--fields` 只读复扫，`semantic_scan_completed=true`、`ready_for_current_stage=false`，实体总数不变。相对 4b：

| 类别 | 4b 涉及实体／发现次数 | 5a 涉及实体／发现次数 |
| --- | ---: | ---: |
| `ExporterFieldGap` | 2,185／32,403 | 1,695／7,486 |
| `UnmappedSemanticField` | 4,610／13,622 | 4,609／10,369 |
| `StageValidation` | 3,878／8,631 | 3,878／8,631 |
| `BodyFidelity` | 490／490 | 490／490 |

字段级非默认分布暴露了下一段真正要保留的内容：来源 URL 425 条、非零排序 1,471 条、待办 40 篇（截止时间 16、完成时间 38）、非空资源 `filename` 2,945 条，以及 note `source`/`source_application` 各 1,664 条。坐标非默认也确实存在：latitude/longitude 各 626，altitude 490；不能一律当作零。`--fields` 只打印已知字段名、数量和处理类别，不打印标题、正文或属性值。单次复扫 74.38 秒，最大 RSS `55,918,592` 字节（约 53.3 MiB）；原备份 SHA 前后仍为 `1d0feb4fe025379db3a869bc348d0769de4b9b23b3a752580d0eb522c607d6cd`，临时子目录及空父目录已清理。

此轮减少的是 **24,917 次默认字段假性 gap**，不是成功迁入 24,917 条数据。下一阶段需要按真实内容价值处理来源/排序/待办/资源文件名及 490 篇正文保真，仍不得跳过 1 处缺失内部引用、1 张 14,050,700 字节超限 PNG 和混合父文件夹。
